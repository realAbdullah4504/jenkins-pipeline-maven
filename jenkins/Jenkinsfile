pipeline {
  agent { label 'agent' }
  stages {
    stage('Build') {
      steps {
        sh 'maven clean package'
        }
        post {
          success {
            archiveArtifacts artifacts: '*.jar', fingerprint: true
            }
          }
      }
    }
}