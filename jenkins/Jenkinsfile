pipeline {
  agent { label 'agent' }
  stages {
    stage('Build') {
      steps {
        sh 'mvn clean package'
        }
        post {
          success {
            archiveArtifacts artifacts: '*.jar', fingerprint: true
            }
          }
      }
    }
}